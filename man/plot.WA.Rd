% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{plot.WA}
\alias{plot.WA}
\title{Plot while-alive trajectory or a covariate's time-varying effect}
\usage{
\method{plot}{WA}(
  x,
  newdata,
  t_seq,
  id = 1,
  mode = c("wa", "cov"),
  covariate = NULL,
  ylab_wa = "While-alive loss rate",
  ylab_cov = NULL,
  xlab = "Time",
  level = 0.95,
  smooth = FALSE,
  span = 0.3,
  ...
)
}
\arguments{
\item{x}{A \code{"WA"} object.}

\item{newdata}{Data used to rebuild the RHS design (same columns as in the model).}

\item{t_seq}{Times to plot over (numeric vector).}

\item{id}{Row index of \code{newdata} to use for the while-alive trajectory (mode = "wa").}

\item{mode}{\code{"wa"} to plot the while-alive loss rate, or \code{"cov"} to
plot a specific covariate's time-varying effect.}

\item{covariate}{Character; covariate name (must appear on RHS) when \code{mode="cov"}.}

\item{ylab_wa}{Y-axis label for while-alive plot.}

\item{ylab_cov}{Y-axis label for covariate-effect plot; default
\code{"Effect of <covariate> on \u03B7(t)"}.}

\item{xlab}{X-axis label.}

\item{level}{Confidence level for ribbons (default 0.95).}

\item{smooth}{Logical; if \code{TRUE}, apply LOESS smoothing to the displayed curve/CI.}

\item{span}{LOESS span used when \code{smooth=TRUE}.}

\item{...}{Unused.}
}
\value{
A \pkg{ggplot2} object.
}
\description{
Plot while-alive trajectory or a covariate's time-varying effect
}
\examples{
\dontrun{
plot.WA(fit, newdata = crt_dt,
        t_seq = seq(0, max(fit$tau_grid), length.out = 200),
        id = 1, mode = "wa", smooth = TRUE)

plot.WA(fit, newdata = crt_dt,
        t_seq = seq(0, max(fit$tau_grid), length.out = 200),
        id = 1, mode = "cov", covariate = "numchron",
        ylab_cov = "Effect of numchron on linear predictor",
        smooth = TRUE)
}
}
